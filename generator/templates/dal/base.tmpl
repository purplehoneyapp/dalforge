/*
NOTE! This code is autogenerated.
Don't manually change this code.
Version [{{.TemplateVersion}}]
*/
package dal

{{- $entityStructName := pascalCase .Name }}
{{- $entityTableName := snakeCase .Name }}
{{- $entityArgumentName := camelCase .Name }}

import (
    "context"
    "encoding/gob"
    "database/sql"
    _ "embed"
    "errors"
    "strings"

    "fmt"
    "time"
    {{if hasJSONColumn .Columns}}
	"encoding/json"
	{{end}}

    log "github.com/sirupsen/logrus"
    "github.com/patrickmn/go-cache"
    "github.com/sony/gobreaker"
)

func init() {
	gob.Register({{$entityStructName}}{})
}

// Struct representing {{$entityStructName}}. Do not use these structs directly across services as this struct carries db specific information.
// recommendation: create higher level service layer structs that might look same or very similar instead.
type {{$entityStructName}} struct {
    ID        int64 `json:"id"` // Auto-incremented number
    Version   int32 `json:"version"` // Only change this value directly in very specific cases.  
    {{range $colName, $col := .Columns}}
    {{$colName | pascalCase}} {{toGoType $col.Type .AllowNull}} `json:"{{$colName | snakeCase}}"`{{end}}

    Created   time.Time `json:"created"`
    Updated   time.Time `json:"updated"`
{{if .Operations.SoftDelete}}
    DeletedAt *time.Time `json:"deleted_at"`
{{end}}
}

// {{$entityStructName}}Repository defines the interface for the {{$entityStructName}}.
// Use this interface in your services to easily mock the database.
type {{$entityStructName}}Repository interface {
    CreateTable(ctx context.Context) error
    GetByID(ctx context.Context, id int64) (*{{$entityStructName}}, error)

    InvalidateCache(entity *{{$entityStructName}})
    FlushListCache()
    FlushAllCache()

{{- if and .Operations .Operations.Write}}
    Create(ctx context.Context, entity *{{$entityStructName}}) (*{{$entityStructName}}, error)
    CreateBulk(ctx context.Context, entities []*{{$entityStructName}}) ([]*{{$entityStructName}}, error)
    Update(ctx context.Context, entity *{{$entityStructName}}) error
{{- end}}

{{- if and .Operations .Operations.Delete}}
    Delete(ctx context.Context, entity *{{$entityStructName}}) error
{{- end}}
{{- if and .Operations .Operations.SoftDelete}}
    HardDelete(ctx context.Context, entity *{{$entityStructName}}) error
{{- end}}

{{- if and .Operations .Operations.Gets }}
    {{- range .Operations.Gets }}
        {{- if ne . "id"}}
            {{- $col := index $.Columns . }}
    GetBy{{pascalCase .}}(ctx context.Context, {{camelCase .}} {{toGoType $col.Type $col.AllowNull}}) (*{{$entityStructName}}, error)
        {{- end }}
    {{- end }}
{{- end }}

{{- /* Bulk Gets Interface */ -}}
{{- if and .Operations .Operations.GetsBulk }}
    {{- range .Operations.GetsBulk }}
    GetBy{{. | pluralize | pascalCase}}(ctx context.Context, {{bulkFuncParams . $.Columns}}) ([]*{{$entityStructName}}, error)
    {{- end }}
{{- end }}

{{- /* Bulk Updates Interface */ -}}
{{- if and .Operations .Operations.UpdatesBulk }}
    {{- range .Operations.UpdatesBulk }}
    {{pascalCase .Name}}(ctx context.Context, {{bulkUpdateFuncParams . $.Columns}}) error
    {{- end }}
{{- end }}

{{- /* Bulk Lists Interface */ -}}
{{- if and .Operations .Operations.ListsBulk }}
    {{- range .Operations.ListsBulk }}
    {{pascalCase .Name}}(ctx context.Context, {{bulkFuncParams .WhereIn $.Columns}}) ([]*{{$entityStructName}}, error)
    {{- end }}
{{- end }}

{{- if and .Operations .Operations.Lists }}
    {{- range .Operations.Lists }}
    {{pascalCase .Name}}(ctx context.Context, {{listFuncParams . $.Columns}}) ([]*{{$entityStructName}}, error)
    Count{{pascalCase .Name}}(ctx context.Context, {{countFuncParams . $.Columns}}) (int64, error)
    {{- end }}
{{- end }}

{{- $softDelete := .Operations.SoftDelete -}}
{{range .Operations.Deletes}}
    {{- $funcParams := deleteFuncParams . $.Columns}}
    {{pascalCase .Name}}(ctx context.Context{{if $funcParams}}, {{$funcParams}}{{end}}) (int64, error)
    {{- if $softDelete}}
    Hard{{pascalCase .Name}}(ctx context.Context{{if $funcParams}}, {{$funcParams}}{{end}}) (int64, error)
    {{- end}}
{{end}}
}

type {{$entityArgumentName}}Repository struct {
    dbProvider          DBProvider
    cache               *cache.Cache  // Simple TTL cache for single items, for inmemory cache management
  	listCache           *cache.Cache // Cache for lists. Simple TTL cache, for inmemory cache management
    countCache          *cache.Cache // Cache for counts. Simple TTL cache, for inmemory cache management
    cacheProvider       CacheProvider
    configProvider      ConfigProvider
	dbBreaker           *gobreaker.CircuitBreaker
    telemetryProvider TelemetryProvider
}

// New{{$entityStructName}}Repository now returns the {{$entityStructName}}Repository interface.
func New{{$entityStructName}}Repository(
    provider DBProvider, 
    cacheProvider CacheProvider,
    configProvider ConfigProvider, 
    dbSettings gobreaker.Settings, 
    telemetry TelemetryProvider) {{$entityStructName}}Repository {

    if configProvider == nil {
        configProvider = DefaultConfigProvider{}
    }

    if cacheProvider == nil {
        cacheProvider = NoopCacheProvider{}
    }

    if telemetry == nil {
		telemetry = NoopTelemetryProvider{}
	}

    if dbSettings.Name == "" {
        dbSettings.Name = "{{$entityTableName}}_dal"
    }
    if dbSettings.ReadyToTrip == nil {
        dbSettings.ReadyToTrip = func(counts gobreaker.Counts) bool {
            return counts.ConsecutiveFailures > {{.CircuitBreaker.ConsecutiveFailures}}
        }
    }
    if dbSettings.OnStateChange == nil {
		dbSettings.OnStateChange = OnCircuitBreakerStateChange
	}

    dbSettings.IsSuccessful = func(err error) bool {
		if err == nil {
			return true
		}
		// If the record just doesn't exist, the database is perfectly healthy
		if errors.Is(err, ErrNotFound) {
			return true
		}
		return false
	}


    if dbSettings.Timeout == 0 {
        dbSettings.Timeout = time.Second * {{.CircuitBreaker.TimeoutSeconds}}
    }

    // Default expiration is 5 minutes and cleanup of 10 minutes
   	singleCache := cache.New(5*time.Minute, 10*time.Minute)
	listCache := cache.New(5*time.Minute, 10*time.Minute)
    countCache := cache.New(5*time.Minute, 10*time.Minute)

    newDAL := &{{$entityArgumentName}}Repository{
        dbProvider:     provider,
        cache:          singleCache,
        listCache:      listCache,
        countCache:     countCache,
        cacheProvider:  cacheProvider,
        configProvider: configProvider,
		dbBreaker:      gobreaker.NewCircuitBreaker(dbSettings),
        telemetryProvider: telemetry,
    }

    // initialize cache invalidation handler
    cacheProvider.OnCacheInvalidated("{{$entityTableName}}", newDAL.onCacheInvalidated)
    cacheProvider.OnCacheFlushList("{{$entityTableName}}", newDAL.onCacheFlushList)
    cacheProvider.OnCacheFlushItem("{{$entityTableName}}", newDAL.onCacheFlushItem)

    return newDAL
}

func (d *{{$entityArgumentName}}Repository) getCacheKey(id int64) string {
    return fmt.Sprintf("{{$entityTableName}}_id:%d", id)
}

{{template "invalidate_cache" (dict "Root" $ "ColumnName" "id")}}
{{template "create_table" (dict "Root" $ "ColumnName" "id")}}

{{- if and .Operations .Operations.Write}}
{{template "create" (dict "Root" $ "ColumnName" "id")}}
{{template "create_bulk" (dict "Root" $ "ColumnName" "id")}}
{{template "update" (dict "Root" $ "ColumnName" "id")}}
{{- end}}

{{- if and .Operations .Operations.Delete}}
{{template "delete" (dict "Root" $ "ColumnName" "id")}}
{{- end}}

{{template "get_by_id" .}}
{{- if and .Operations .Operations.Gets }}
    {{- range .Operations.Gets }}
        {{- if ne . "id"}}
{{template "get_operation" (dict "Root" $ "ColumnName" .)}}
        {{- end }}
    {{- end }}
{{- end }}

{{- if and .Operations .Operations.Lists }}
    {{- range .Operations.Lists }}
{{template "list_operation" (dict "Root" $ "List" .)}}
    {{- end }}
{{- end }}

{{- if and .Operations .Operations.Lists }}
    {{- range .Operations.Lists }}
{{template "count_operation" (dict "Root" $ "List" .)}}
    {{- end }}
{{- end }}

{{- if and .Operations .Operations.GetsBulk }}
    {{- range .Operations.GetsBulk }}
{{template "get_bulk_operation" (dict "Root" $ "ColumnName" .)}}
    {{- end }}
{{- end }}

{{- if and .Operations .Operations.UpdatesBulk }}
    {{- range .Operations.UpdatesBulk }}
{{template "update_bulk_operation" (dict "Root" $ "UpdateConfig" .)}}
    {{- end }}
{{- end }}

{{- if and .Operations .Operations.ListsBulk }}
    {{- range .Operations.ListsBulk }}
{{template "list_bulk_operation" (dict "Root" $ "ListBulk" .)}}
    {{- end }}
{{- end }}

{{template "delete_custom" .}}